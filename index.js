export const handler = async (event) => {
    // TODO implement
    const response = {
      statusCode: 200,
      body: JSON.stringify('Habla Dev from github actions!'),
    };
    return response;
  };
  