export const handler = async (event) => {
    // TODO implement
    const response = {
      statusCode: 200,
      body: JSON.stringify('Habla Dev from github actions for a second time!'),
    };
    return response;
  };
  